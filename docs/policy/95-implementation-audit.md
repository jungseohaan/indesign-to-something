# Source Ownership Policy: Implementation Audit Map

> This file is part of the canonical source ownership policy.
> The canonical index is `docs/specs/POLICY-source-ownership.md`.

## 14. Code Path Audit

This map records the code paths that may touch source ownership. The audit
status is policy-oriented: `conforming` means the path executes Stage 1 facts;
`migration bridge` means the path is allowed temporarily but must not create a
new visible owner; `cleanup target` means the path still contains legacy logic
that must be removed or guarded by validation.

| Path | Stage | Status | Ownership rule |
|---|---|---|---|
| `scripts/indd/extraction_plan_builder.jsx` | E1 / Stage 0-1 input | migration bridge | Creates extraction candidates and source facts. It may prepare candidate material, but final source-slot ownership belongs to ObjectPlan. |
| `scripts/indd/planner_bundles.jsx` | Stage 1 | conforming target | Converts candidates/source clusters into bundle-slot facts. Inline visual candidates with no text ownership use `CONTENT_VISUAL_SLOT`; text shells use `SHELL_SLOT`. |
| `scripts/indd/object_plans.jsx` | Stage 1 | conforming target | Writes the executable ObjectPlan contract. `inlineAnchorSourceObjectId` may affect placement only when `sourceInlineFlow=true`. |
| `scripts/indd/source_slot_registry.jsx` | Stage 1 validation | conforming target | Checks slot identity and duplicate ownership. It must validate visible slot closure, not just direct ids. |
| `scripts/indd/extraction_orchestrator.jsx` | E2 coordinator | migration bridge | Executes only planned passes and passes planned candidate lists into renderers. It must not create replacement candidates. |
| `scripts/indd/render_inline_objects.jsx` | E2 executor | migration bridge | Executes `pass.inline_objects` candidates. It must preserve `INLINE` / `STORY_FLOW` ownership and never recreate missing anchors by scanning stories after planning. |
| `scripts/indd/render_master_graphics.jsx` | E2 executor | migration bridge | Executes applied-master page/floating candidates. Master-origin story-flow inline material may use a master direct-export file only when the result is stamped back to the inline candidate. |
| `scripts/indd/render_groups.jsx` / decoration group helpers | E2 executor | migration bridge | Executes planned textless image groups. It must not use overlap, color, or occlusion to decide visible ownership after planning. |
| `scripts/indd/render_images.jsx` | E2 executor | migration bridge | Executes planned placed-image/frame candidates. Clip-carrying parents, not raw child images, own clipped visible slots. |
| `converter/.../ownership/OwnershipPlanner.java` | Stage 1 / Java bridge | cleanup target | Consumes ObjectPlan and applies remaining Java normalization. It may validate and execute planner-declared facts, but bridge-added or bridge-mutated ownership is a planning defect. |
| `converter/.../ownership/OwnershipPlanValidator.java` | Stage 4 | conforming target | Must fail duplicate visible slots, inline/floating duplicates, source-less visible visuals, and visible master owner for story-flow inline source slots. |
| `converter/.../ResolvedBuildContext.java` | Stage 2-3 lookup | cleanup target | Must resolve rendered material by exact plan/source-slot identity. Alternate placement/source-only fallback lookups are not V2 ownership policy. |
| `converter/.../phase4/TableBuilder.java` | Stage 2-3 table executor bridge | cleanup target | Must execute Stage 1 table-structure ownership only. Same-story nested tables stay inside the parent HWPX cell, and wrapper/nested table relationships must not reappear as separate page-level tables or PNG fallback. |
| `converter/.../InlineFrameHandler.java` | Stage 2 text executor | cleanup target | Must execute planned inline anchors only. It must not turn floating/page material into inline material or synthesize missing inline owners. |
| `converter/.../FramePlacer.java` and visual placement helpers | Stage 3 visual executor | cleanup target | Must place planned visuals only and map HWPX plane from ObjectPlan. It must not repair z-depth from bounds, pixels, or observed overlap. |
| `converter/.../VisualShellPreparationRules.java` | Stage 3 image prep | cleanup target | May prepare planned textless material, but must not decide shell/background/content ownership from visual symptoms. |
| `converter/.../HwpxImageBuilder.java` / `ASTToHwpxConverter.java` | Stage 3 writer | cleanup target | Must preserve Stage 1 placement/layer/z-order. It may translate to HWPX primitives but must not create a new foreground/background role band. |

## 15. Current Known Policy Checks

- ObjectPlan validation is a pre-render gate in `scripts/indd/extraction_orchestrator.jsx`.
- Extraction result validation rejects unplanned or mismatched result rows before
  HWPX conversion.
- `pass.master_page_graphics` receives both master and inline candidates so it
  can route master direct-export bytes back to the inline owner when the source
  slot is story-flow inline.
- `OwnershipPlanner.isStoryFlowInlineVisualMaterialSlot` excludes master-page
  plans from late inline normalization. Master-origin inline material must be
  declared as inline before this Java path, not inferred afterward.

## 16. Remaining Cleanup Targets

- Remove or hard-fail any Java legacy bridge path that adds or mutates
  ObjectPlans instead of reporting a planning defect.
- Remove alternate plan lookup strategies that search by source-only,
  inline/floating alternates, overlap score, or file existence after Stage 1.
- Replace any executor-local foreground/background role lists with the
  canonical three-strata mapping:
  `BACKGROUND_GRAPHIC < TEXTLESS_IMAGE_GROUP < TEXT_TABLE_STRUCTURE`.
- Promote duplicate/missing source-slot warnings to blocking validation in
  dev/test conversion paths.
- Keep master grouping limited to page/floating master fragments. Story-flow
  inline material authored on a master spread stays inline.

## 17. Active Policy Alignment Audit

This section tracks code paths found during the policy-alignment pass whose
behavior can hide Stage 1 defects or re-open ownership decisions after
planning. The status terms are:

- `confirmed violation`: conflicts with the canonical policy and should be
  removed or converted into validation.
- `cleanup target`: still acceptable only as migration scaffolding, but must
  not become new behavior.
- `needs classification`: requires source/object-plan diagnostics before a
  safe edit.

| Path | Status | Policy conflict | Direction |
|---|---|---|---|
| `scripts/indd/execution_candidates.jsx` `_applyObjectPlanExecutionFields` | confirmed violation / fixed | Reconstructed missing `coordinateSpace` from `placement` in the execution bridge. That hid a required Stage 1 ObjectPlan field. | Removed the fallback. Missing `coordinateSpace` now remains visible to execution-candidate contract diagnostics and ObjectPlan validation. |
| `scripts/indd/execution_candidates.jsx` `_buildExecutionCandidatesFromObjectPlans` | confirmed violation / fixed | Copied source candidates into execution rows even when no matching ObjectPlan existed, when the matched ObjectPlan lacked required execution fields, or when `contractStatus` was still `NEEDS_MIGRATION_POLICY`. That let transitional candidates survive as visible material without a Stage 1 ownership contract. | The bridge now emits execution rows only for candidates backed by `READY_FOR_STAGE1_IMPORT` ObjectPlans with a complete execution contract. Skipped rows are recorded in `objectPlanDiagnostics.executionCandidateBridge` and summarized in `objectPlanSummary.executionCandidateBridge`, including non-ready contract status and migration blocker details. |
| `converter/.../ResolvedToASTBuilder#importPlannerDeclaredObjectPlans` | fixed / monitor | Planner-declared JSON import used enum fallbacks for `placement`, `coordinateSpace`, `materialization`, and `visualLayer`, which could turn an incomplete ObjectPlan into executable defaults. | Planner-declared import now uses strict enum parsing for required execution fields and writes `STAGE1_PLANNER_DECLARED_OBJECT_PLAN_IMPORT_CONTRACT_INVALID` when importable plans are missing those fields. ObjectPlan constructor defaults were split into explicit legacy-only factories. |
| `converter/.../ResolvedToASTBuilder#normalizeClosedInlineCarrierFlowTextPlans` | fixed / monitor | Mutated imported HWPX text plans from `FLOATING/PAGE` to `INLINE/STORY_FLOW` after ObjectPlan import using rendered-group/source-bounds inspection. This was a post-plan inline/floating flip. | The post-import mutation was removed. Mismatched closed inline carrier text plans now emit `STAGE1_IMPORTED_PLAN_INLINE_FLOW_TEXT_PLACEMENT_MISMATCH`; the placement decision must be fixed in Stage 1 ObjectPlan generation. |
| `converter/.../ResolvedToASTBuilder` and `phase3/InlineFrameHandler` closed inline carrier flow order | fixed / monitor | Reconstructed inline carrier visual/text fragment order from rendered group hidden ids plus TextFrame/source geometry. That let Stage 2/3 decide before/after ordering after Stage 1 and created a bounds-based fallback for missing ObjectPlan facts. | Stage 1 now emits `inlineSourceTreeClosed` and `inlineFlowSourceObjectIds` on ObjectPlans. Java import/execution consumes that ordered source list only; missing order with hidden child sources emits `STAGE1_CLOSED_INLINE_CARRIER_FLOW_ORDER_MISSING` or `STAGE3_CLOSED_INLINE_CARRIER_FLOW_ORDER_MISSING`. Geometry-based order reconstruction was removed from the executor path. |
| `converter/.../ResolvedToASTBuilder#renderedGroupByPlanSource` | fixed / monitor | Fell back from candidate id/page-id matching to source-set matching to find rendered material. Policy requires exact plan/source-slot identity rather than alternate source-only lookup. | Source-set rendered material fallback was removed. Planner-declared rendered plans now require exact candidate id or page/primary-source material and otherwise emit `STAGE1_PLANNER_DECLARED_OBJECT_PLAN_IMPORT_CONTRACT_INVALID`. |
| `converter/.../ResolvedToASTBuilder#removeStage1OwnershipInvariantWarnings` | fixed / monitor | Removed earlier ownership invariant warnings before Stage 4 validation. This could make duplicate/missing ownership defects look clean. | The removal hook was deleted. Stage 1 producer diagnostics and Stage 4 validation diagnostics are both preserved in `ownership-warnings.jsonl`. |
| `converter/.../ObjectPlan` constructor defaults | fixed / monitor | Defaults missing `visualLayer`, `materialization`, and `coordinateSpace`, which weakened required-field validation. | The innermost/copy constructors preserve `null`, and short public constructors were replaced by explicit `legacyDefaulted` factories. Main planner/import paths use full ObjectPlan fields; remaining `legacyDefaulted` use is limited to test fixtures. |
| `converter/.../ResolvedBuildContext#findOwnershipPlanForRendered` / text-frame lookup helpers | fixed / monitor | Rendered lookup searched both the rendered group's placement and the opposite inline/floating placement, then preferred the visible plan. TextFrame lookup also mixed "any text-frame owner" with "HWPX text owner" in one fallback-returning method. | Alternate placement lookup was removed. Render-unit fallback now also requires the plan placement to match the rendered group's placement. TextFrame lookup is split into explicit `findAnyTextFrameOwnershipPlan` for skip/execution gating and `findHwpxTextFrameOwnershipPlan` for HWPX text placement checks, so non-HWPX text owners are not accidentally treated as HWPX text plans. |
| `converter/.../OwnershipPlanner#runLegacyOwnershipMutationBridge` | fixed / monitor | Java-side migration bridge could add or mutate ObjectPlans after imported/planned Stage 1 facts, hiding missing source ownership declarations and re-opening ownership decisions in Java. | The legacy mutation bridge is now disabled by default and emits `STAGE1_LEGACY_OWNERSHIP_BRIDGE_SKIPPED`. It can only run by explicit opt-in (`-Didml.ownership.runLegacyBridge=true` or `IDML_RUN_LEGACY_OWNERSHIP_BRIDGE=true`, unless the older skip flag is set). Missing ownership must be fixed in Stage 1 ObjectPlan generation rather than repaired by the bridge. |
| `converter/.../OwnershipPlanner#ensureOwnedTextFramePlansForVisibleTextShells` | fixed / monitor | After the legacy bridge, Java could still create new `HWPX_TEXT` ObjectPlans for text frames referenced by visible text-shell plans. That hid missing text-slot ownership in Stage 1. | The method now emits `SHELL_OWNED_TEXT_FRAME_MISSING_HWPX_TEXT_PLAN` and records a missing-plan metric instead of adding a synthetic text plan. A visible shell that owns editable text must arrive with its corresponding HWPX text ObjectPlan already declared. |
| `converter/.../OwnershipPlanner#finalizeFloatingTextShellBackPlaneContracts` | confirmed violation / removed | Overlap with non-owned text/content could change a floating text shell from `LABEL_BACKDROP` to `CONTAINER_BACKDROP`, drop its text ownership, clear owned TF ids, and reset z-order. That was a bounds/overlap-based ownership and layer rewrite after planning. | The overlap/depth monitor and its suppressed mutation path were removed. Back-plane/layer intent must be declared by Stage 1 source ownership facts and then checked by validation, not inferred from visible overlap in Java. |
| `converter/.../InlineFrameHandler`, `StoryConverter`, `StoryLoader`, and `StoryFlowAssembler` fallback paths | cleanup target / partially fixed | Several methods still search rendered groups/plans around inline anchors and can behave like synthetic inline recovery. | `InlineFrameHandler` no longer matches shell material by source-id overlap or descendant-tree inference when exact plan file/render/dom/source identity is missing, and inline PNG execution now requires `ObjectPlan.file`/`ObjectPlan.bounds`. Nested inline anchors inside `tryInlineTextFrameAsItems` now execute only through `loadPlannedInlineAnchorItems`; without a Stage 1 plan they emit `STAGE2_UNPLANNED_NESTED_INLINE_ANCHOR_SKIPPED` instead of trying legacy group/text/table/empty-box/spacer materializers. Legacy shape/group editable-child shell synthesis now refuses any anchor bundle that already has a Stage 1 ObjectPlan, not only direct executable inline plans. `StoryConverter` TF inline visuals also execute from `ObjectPlan.file`/`ObjectPlan.bounds` only. `StoryLoader` table-cell inline graphic rescue now refuses paragraph fallback when the inline plan has no bounds or no source-derived paragraph reference; this exposes a Stage 1/source metadata defect instead of silently inserting material at the anchor paragraph. `StoryLoader` leading-inline insertion and `StoryFlowAssembler` cell-flow assembly now consume `loadPlannedInlineAnchorItems` only, so callers no longer retry direct text-shell/image loaders after planned inline execution fails. |
| `converter/.../phase3/InlineFrameHandler` legacy editable-child shell synthesizers | fixed / monitor | Legacy helpers such as `tryInlineGroupAsBoxList`, `tryInlineShapeWithEditableChildAsShell`, `tryInlineGroupShellWithEditableChild`, `tryInlineGroupAsSingleBadge`, and `buildChildEditableBoxes` could still synthesize inline text-shell/badge material from source geometry and rendered metadata if called for an unplanned anchor while other Stage 1 ObjectPlans existed. | These helpers now return immediately whenever Stage 1 ObjectPlans are present. Planned inline shells must execute through direct ObjectPlan paths; unplanned anchors remain Stage 1 coverage defects instead of falling back to geometry/stroke/overlap synthesis. The unused shifted complete-label inline/page pair detector was removed so page-width offset matching cannot be revived as an executor-side inline/floating repair. |
| `converter/.../FramePlacer` / `BackgroundInjector` / `VisualPlacement*` | cleanup target / partially fixed | Visual executors still contain skip/recovery decisions and rendered-material matching. | `BackgroundInjector` now requires executable `ObjectPlan.file`, `ObjectPlan.bounds`, and `CoordinateSpace.PAGE` instead of falling back to rendered file/bounds or normalizing spread/page-local coordinates in Stage 3. `ABSORB_TEXT_STYLE` plans are routed to the text-emphasis executor instead of being rejected as dropped visuals. `FramePlacer` no longer recovers text-shell ownership from rendered `editableTextFrameIds`; shell/style decisions must come from ObjectPlan indexes. The legacy non-editable TextFrame rescue path that promoted unplanned text based on story text, rendered-channel presence, rotated parent rectangles, or badge child render types was removed; unplanned non-editable TFs are skipped with `STAGE2_UNPLANNED_NON_EDITABLE_TEXT_FRAME_SKIPPED` so Stage 1 must declare any visible HWPX text owner. The concept-diagram protection set, which was populated from text length, numbered-prompt patterns, parentless TF status, and geometry proximity, was removed from `FramePlacer`, `ASTRunConverter`, `InlineFrameHandler`, and `OwnershipPlanner`; concept-like layout must be represented by ordinary source ownership/ObjectPlan facts. FramePlacer native fill/style absorption no longer reads `RenderedGroup.nativeFillChildIds()` or records executor-local `nativeFillAbsorbedIds`; it applies sibling shape style only from `ObjectPlan.styleSourceObjectIds`. Synthetic `GraphicLine` fallback is limited to a visible floating parent plan plus a child `NATIVE_SOURCE_SHAPE` line plan that references the original source id, so Stage 3 no longer creates child line material from a merely-empty PNG symptom. Remaining work: classify remaining geometry preparation helpers in text placement as Stage 1-owned materialization or explicit validation. |
| `converter/.../phase2/FramePlacer` text-frame geometry | cleanup target / partially fixed | Floating HWPX text placement still had executor-local geometry correction that could override Stage 1 text placement facts for anchored-table or object-replacement-only frames. | `FramePlacer` now prefers executable bounds from `HWPX_TEXT` ObjectPlans with `CoordinateSpace.PAGE`; when those planned bounds are present, later page-relative overrides do not replace them. Missing executable bounds on a floating HWPX text plan emit `STAGE2_TEXT_PLAN_MISSING_EXECUTABLE_BOUNDS` while the legacy TextFrame geometry bridge remains for existing coverage. |
| `converter/.../phase3/SimpleButtonLabelInlineFactory` | fixed / monitor | The inline button-label executor re-read anchor/shell `geometricBounds` or `pageRelativeBounds` to size the inline drawText object, even though Stage 1 had already declared a simple-button ObjectPlan. | The factory now sizes the inline text-shell from the corresponding simple-button ObjectPlan bounds only. This preserves the HWP table-in-table workaround while keeping shell/text ownership and geometry in Stage 1. |
| `converter/.../stage3/VisualTfInlineCompositor` | fixed / monitor | Parent PNG placement bounds were expanded in Stage 3 by unioning unowned inline child visual bounds, and child compositing was allowed when no separate visible plan existed. That could change executable geometry after ObjectPlan planning. | Parent placement bounds are no longer union-expanded during execution. Inline child compositing is allowed only when the child source id is included in the parent ObjectPlan source/visual source set and no other visible ObjectPlan owns it. |
| `converter/.../phase4/TableBuilder` whole-table PNG fallback | fixed / monitor | Legacy table conversion could create a whole-table PNG from rendered metadata or page-background crop when an inline object was detected, effectively replacing editable table/text ownership outside ObjectPlan. | Whole-table PNG execution now requires a matching `PLACE_FLOATING_PNG` ObjectPlan with PNG materialization, and the page-background crop fallback was removed. Missing planned PNG material falls back to ASTTable instead of creating a new visible owner. |
| `converter/.../phase4/TableBuilder` table decoration absorption | fixed / monitor | Legacy helpers could project page-object rectangles/lines into HWPX cell fill/border by matching arbitrary source bounds against table grid geometry. That re-opened ownership in the executor. | TableBuilder now executes only table style source ids declared by Stage 1 `ObjectPlan.styleSourceObjectIds` on a `PLACE_TABLE_STYLE` / `HWPX_TABLE_STYLE` plan. Geometry is used only to map an already-declared style source to the affected HWPX cells; it is not used to discover ownership. If table decoration is missing, Stage 1 must add the source object to the table plan's `TABLE_STYLE_SLOT` or emit a separate visual owner. |
| `converter/.../phase3/StoryConverter#tfInlineVisualIdsForStory` | fixed / monitor | TF inline visual restoration skipped ids listed in rendered `nativeFillChildIds`, reusing extractor metadata as an executor-local ownership suppression rule. | Inline visual restoration now uses ObjectPlan presence/action only. Style/fill absorption must be represented by ObjectPlan style ownership rather than rendered `nativeFillChildIds`. |
| `converter/.../phase3/StoryConverter` / `StoryLoader` unplanned inline anchors | fixed / monitor | When no ObjectPlan existed for an inline anchor, the story executors still tried a sequence of legacy materializers (`tryInlineGroup*`, `tryInlineTextFrame*`, direct PNG load, spacer creation), creating visible inline owners after Stage 1. | Unplanned inline anchors now emit `STAGE2_UNPLANNED_INLINE_ANCHOR_SKIPPED` and do not synthesize material in either IDML or resolved story paths. Inline text/shell/PNG/spacer output must be declared by Stage 1 ObjectPlan paths. |
| `converter/.../ASTTableConverter#reorderInlineObjectsByBoundsX` call sites | fixed / monitor | Table/story executors reordered paragraph inline items by rendered `boundsX`, moving inline objects around text after source story order and ObjectPlan import. That reopened story-flow placement using visual geometry. | The bounds-X reorder helper is now legacy-only. `ASTTableConverter`, `StoryLoader`, and `StoryConverter` skip it whenever Stage 1 ObjectPlans are present, so source story/anchor order remains the executable contract. |
| `converter/.../StoryConverter` / `StoryLoader` page-number role heuristics | fixed / monitor | Decorative leader/page-number handling treated a small inline TextFrame containing only digits as a page-number frame. That used literal text plus frame geometry to assign semantic role after Stage 1. | When Stage 1 ObjectPlans are present, page-number role is accepted only from source metadata such as the InDesign auto-page marker (`\u0018`) or an explicit page-number object style. Digit/bounds heuristics are legacy-only and otherwise emit `STAGE2_PAGE_NUMBER_ROLE_HEURISTIC_SUPPRESSED`. |
| `converter/.../StoryConverter#preserveComposedLineBreaksForTrailingAnswerVisuals` | fixed / monitor | Text execution inserted composed-line breaks only when the last source line looked like an answer blank and a rendered floating visual overlapped the line. That used rendered overlap to decide a text layout rewrite after Stage 1. | The rendered-overlap line-break heuristic is now legacy-only. When Stage 1 ObjectPlans are present, candidates are reported as `STAGE2_TRAILING_ANSWER_LINE_BREAK_HEURISTIC_SUPPRESSED`; source line-break preservation for answer visuals must be declared by Stage 1 source ownership/style facts before Text Builder executes it. |
| `converter/.../DoviraSubunitMarkerPolicy` duplicate marker suppression | fixed / monitor | The resolved/story executors dropped leading inline markers for a specific paragraph style (`01_도비라-소단원명`) when another story on the page contained similar title text and a floating marker with nearly identical bounds. That combined style-name, text, and coordinate heuristics to suppress visible material after Stage 1. | Duplicate marker suppression is now legacy-only. If Stage 1 ObjectPlans are present, `isDuplicateMarkerStory` returns false, so marker visibility must be decided by ObjectPlan `DROP_VISUAL` / text ownership facts rather than page/style/text/bounds heuristics in StoryConverter, StoryLoader, or ASTRunConverter. |
| `converter/.../ASTRunConverter#processInlineGraphic` | fixed / monitor | The IDML run bridge could execute planned inline material first, then fall through to legacy group shell/box/badge/rendered-PNG/vector synthesis when a Stage 1 plan was missing, non-executable, or the inline graphic id could not be parsed. | When resolved Stage 1 ObjectPlans are present, `processInlineGraphic` now returns after planned execution fails instead of synthesizing alternate visible inline material. Legacy-only conversion without ObjectPlans keeps its existing fallback path until that surface is separately migrated. |
| `converter/.../phase3/InlineFrameHandler#loadInlineObject` | fixed / monitor | Planned inline PNG execution still corrected width/height from rendered bounds, PNG pixel ratio, stroke weight, and TextFrame geometry, which could override executable ObjectPlan bounds after planning. | Inline PNG execution now requires `INLINE` / `STORY_FLOW` ObjectPlans with `file` and `bounds`; the resulting inline object size and ordering coordinates come from plan bounds only. |
| `converter/.../stage3/VisualBuilder#pageRelativeNativeShapeBounds` | fixed / monitor | Native source-shape execution used `ResolvedPageItem.pageRelativeBounds()` / `geometricBounds()` when the ObjectPlan had no executable bounds. That let Stage 3 synthesize missing geometry and hid incomplete native-shape plans. | Native source-shape execution now requires `ObjectPlan.bounds` with `CoordinateSpace.PAGE`; missing or non-page coordinates are counted as skips instead of being repaired from source item geometry. |
| `converter/.../phase6/BackgroundInjector` visual crop contract | fixed / monitor | Stage 3 treated `ObjectPlan.renderSourceBounds` as if it were an executable crop contract. That cropped page-local PNGs from diagnostic render bounds and then placed the cropped pixels in the original planned bounds, enlarging the remaining content; it also missed spread-cross crops when only `RenderedGroup.cropSourceBounds` carried the explicit extractor crop contract. | `ObjectPlan.cropSourceBounds` is now the first-class executable crop contract. Stage 3 uses it when present and falls back only to legacy explicit `RenderedGroup.cropSourceBounds()`. `ObjectPlan.renderSourceBounds` remains diagnostic/source provenance and must not drive crop, placement, ownership, or layer decisions. The policy checker fails if crop is aliased into `renderSourceBounds` or if `BackgroundInjector` derives crop from `ObjectPlan.renderSourceBounds`. |
| `converter/.../OwnershipPlanner#resolveNonVisibleFloatingVisuals` | fixed / monitor | Used rendered group bounds when a visible floating ObjectPlan had no bounds, hiding an incomplete plan during Stage 1 cleanup. | Rendered-bounds fallback was removed. Missing bounds now emit `VISIBLE_FLOATING_PLAN_MISSING_BOUNDS` instead of being silently normalized. |
| `converter/.../OwnershipPlanner#restorePageTextlessGraphicGroupContracts` | fixed / monitor | Final Stage 1 cleanup could rewrite page textless graphic group plans into floating content PNG contracts and recover bounds from rendered material. That hid incomplete or incorrectly imported ObjectPlan fields. | The method now emits `PAGE_TEXTLESS_GRAPHIC_GROUP_CONTRACT_REPAIR_SUPPRESSED` and a suppression metric instead of changing text/visual action, layer, placement, coordinate space, materialization, or bounds. Page textless graphic group contracts must be emitted correctly by Stage 1 planning/import. |
| `converter/.../OwnershipPlanner#normalizeInlineOwnedTextShellsToStoryFlow` | fixed / monitor | Final cleanup could flip owned text-shell plans to `INLINE/STORY_FLOW` based on inline source/anchor checks, then align child text-frame plans. This reopened the inline/floating decision after ObjectPlan planning. | The method now emits `INLINE_OWNED_TEXT_SHELL_PLACEMENT_REPAIR_SUPPRESSED` and does not mutate shell or text-frame placement. Inline text-shell flow must be declared by Stage 1. |
| `converter/.../OwnershipPlanner#normalizeDuplicateVisibleSourceSlots` | fixed / monitor | Duplicate visible source slots were resolved by choosing a canonical owner in Java and removing source ids or dropping loser visuals. That could mask duplicate ownership defects and make candidates disappear after planning. | The method now emits `DUPLICATE_VISIBLE_SOURCE_SLOT_REPAIR_SUPPRESSED` with candidate indexes and the canonical scoring result, but leaves all plans intact for validator diagnostics. |
| `converter/.../OwnershipPlanner#normalizeVisibleVisualSourcesToPlanPage` | fixed / monitor | Final cleanup trimmed `sourceObjectIds`, `visualSourceObjectIds`, and descendant ids by plan page and bounds intersection. That rewrote source-bundle identity from page geometry after ownership had already been declared. | The method now emits `VISIBLE_VISUAL_SOURCE_PAGE_REWRITE_SUPPRESSED` with retained-id diagnostics instead of mutating source id arrays. Cross-page or wrong-page source membership must be fixed in Stage 1 source metadata/ObjectPlan generation. |
| `converter/.../OwnershipPlanner#normalizeInlineTextStyleMarkerVisuals` | fixed / monitor | Final cleanup could reinterpret thin inline graphic marks as `ABSORB_TEXT_STYLE`, move visual sources into style sources, and clear visible visual-source slots based on Java-side geometry/style checks. | The method now emits `INLINE_TEXT_STYLE_MARKER_CONTRACT_REPAIR_SUPPRESSED` instead of mutating the plan. Inline text-style marker absorption must be declared by Stage 1 ObjectPlan facts. |
| `converter/.../OwnershipPlanner#normalizeTextShellsWithMaterializedTextOwners` | fixed / monitor | Final cleanup could drop a text-shell plan's text ownership when a separate HWPX text/table-style owner existed, masking a duplicate text-slot ownership defect. | The method now emits `TEXT_SHELL_MATERIALIZED_TEXT_OWNER_REPAIR_SUPPRESSED` and preserves the conflicting plans for validator diagnostics. |
| `converter/.../OwnershipPlanner#normalizeTextlessShellsWithoutOwnedText` | fixed / monitor | Final cleanup could change a text shell with `OWNED_BY_HWPX_TEXT` but no `ownedTextFrameIds` into a visual-only shell. That hid an incomplete shell/text ObjectPlan contract. | The method now emits `TEXTLESS_SHELL_TEXT_ACTION_REPAIR_SUPPRESSED` instead of changing text ownership. Stage 1 must declare visual-only shells as `DROP_TEXT`. |
| `converter/.../OwnershipPlanner#normalizeRawClippedImageVisualSources` | fixed / monitor | Final cleanup could replace raw image visual source ids with clip-parent ids after planning. That changed source-bundle slot identity from source-tree inspection in Java. | The method now emits `RAW_CLIPPED_IMAGE_VISUAL_SOURCE_REPAIR_SUPPRESSED` with both raw and parent source ids. Clipped-image ownership must be represented in Stage 1 source metadata/ObjectPlan generation. |
| `converter/.../OwnershipPlanner#finalizeOwnedTextFrameDepthContracts` | fixed / monitor | Skip-after-bridge depth finalization could raise HWPX text plan `zOrder` above its owning text shell, rewriting the depth contract after Stage 1 planning. | The method now emits `OWNED_TEXT_FRAME_ZORDER_REPAIR_SUPPRESSED` and records a suppression metric instead of mutating `zOrder`. Text-over-shell depth must be declared by Stage 1 and verified by validation warnings. |
| `converter/.../OwnershipPlanner#warnTextShellZOrder` | confirmed violation / fixed | The z-order validator treated TextFrame ids in broad `sourceObjectIds` as shell-owned text. That made ancestry look like text-slot ownership and produced false policy warnings for visual-only `DROP_TEXT` shells. | The validator now checks shell/text depth only for explicit `ownedTextFrameIds`. `sourceObjectIds` remain provenance only and are not used as text ownership evidence. |
| `scripts/indd/planner_bundles.jsx#_plannerBundleMaterialization` | confirmed violation / fixed | Cross-page background fragments were emitted as generic `EXTRACTED_PNG_VECTOR` even when source metadata showed that a source anchored to another applied page intersected the current page. Stage 4 then had to detect that the required page-local fragment contract was missing. | Stage 1 now emits `TEXTLESS_VISUAL_FRAGMENT` for same-spread cross-page `BACKGROUND` material whose adjusted source bounds overlap the target page. The source ids are preserved; only the materialization contract is made explicit before Java import. |
| `converter/.../OwnershipPlanner#canonicalizeImportedPreplannedObjectPlan` | fixed / monitor | Import-time canonicalization rewrote planner-declared ObjectPlans before validation: inline style marks became `ABSORB_TEXT_STYLE`, story-flow inline/page-position plans became `FLOATING/PAGE`, closed inline carriers became inline PNGs, page textless groups became floating content PNGs, and standalone inline visuals had their visual action repaired. | Import now preserves the ObjectPlan exactly and emits `IMPORTED_*_REPAIR_SUPPRESSED` diagnostics for each previously repaired pattern. Imported ObjectPlans must already carry executable ownership, placement, materialization, layer, and source-slot fields. |
| `converter/.../phase3/MathProcessor#findInlinePlaceholderPlan` | fixed / monitor | Formula placeholder detection returned a non-inline fallback plan when no inline `PLACE_INLINE_PNG`/`PLACE_TEXT_SHELL` plan existed for a source id. That could treat a source-owned floating/page plan as inline placeholder context. | The fallback was removed. Formula placeholder handling now requires an explicit inline visual/text-shell ObjectPlan for the source. |
| `converter/.../stage3/VisualSyntheticLinePlacer` | fixed / monitor | Synthetic GraphicLine recovery verified a child `NATIVE_SOURCE_SHAPE` ObjectPlan but still computed placement from `ResolvedPageItem.geometricBounds()` and parent source geometry. That let Stage 3 override executable geometry after planning. | Synthetic line placement now requires the child plan to be `FLOATING` / `CoordinateSpace.PAGE` with executable `ObjectPlan.bounds`, and uses those bounds directly. Source item data is only used for material color/opacity, not geometry. |
| `converter/.../phase6/BackgroundInjector` planner-declared line extent repair | fixed / monitor | Floating visual placement used `ObjectPlan.bounds`, but planner-declared line visuals with zero width/height were expanded in Stage 3 from source `strokeWeight`. That repaired incomplete executable bounds after planning. | The source-stroke extent repair was removed. Zero-size visual bounds now remain non-executable and are skipped by the normal visible-intersection/positive-size checks, forcing Stage 1 to declare usable bounds. |
| `converter/.../phase6/BackgroundInjector` pixel/aspect placement repair | fixed / monitor | Floating visual placement started from `ObjectPlan.bounds`, but white-stroke and complete-PNG cases could expand the final placement bounds from decoded PNG pixel dimensions/aspect ratio. That let Stage 3 repair or reinterpret executable geometry after planning. | Pixel decoding remains allowed for material preparation such as crop, transparency, and pixel metadata, but it no longer mutates final placement bounds. Stage 1 must declare the executable visual bounds for white-stroke and complete-PNG material. |
| `converter/.../stage3/VisualPlacementPlanBuilder` strip-crop placement override | fixed / monitor | Page-anchored strip cropping could write a pixel-derived x/width override into `PreparedVisualImage`, and the placement builder used it instead of the ObjectPlan-derived visible bounds. That made crop preparation mutate final placement geometry. | Strip crop remains a material crop operation only. `PreparedVisualImage` no longer carries placement override fields, and `VisualPlacementPlanBuilder` always builds x/y/width/height from the planned bounds passed by the visual executor. Master strip detection now keys off `planPassId=pass.master_page_graphics` instead of reason strings such as `master_graphic`/`haseera_graphic`. |
| `converter/.../stage3/VisualTextEmphasisAbsorber` target text selection | fixed / monitor | `ABSORB_TEXT_STYLE` execution required a matching ObjectPlan, but still selected the target TextFrame/paragraph by page-wide line-overlap and text fallback. That could apply style ownership to an unplanned text owner after Stage 1. | The absorber now requires non-empty `ObjectPlan.ownedTextFrameIds`, searches composed lines only inside those planned TextFrames, and refuses same-page/story text fallback when the exact planned TextFrame block is absent. Missing target ids are recorded as `SKIP_ABSORB_TEXT_STYLE_MISSING_OWNED_TEXT_FRAME`. |
| `converter/.../stage3/VisualShellPreparationRules` | fixed / monitor | Container-shell PNG knockout was gated by rendered bounds and source fill/bounds heuristics even after ObjectPlan selection. That let Stage 3 decide material preparation from visual symptoms. | Container-shell preparation now requires an explicit shell-slot ObjectPlan signal and uses `ObjectPlan.bounds` for container-size gating. Rendered bounds no longer decide whether a planned PNG is a container shell candidate. |
| `converter/.../phase3/InlineFrameHandler#loadRenderedInlineBadge` | fixed / monitor | Inline badge material lookup required a plan later, but still selected files from `RenderedGroup.file()` and resized the inline object from `RenderedGroup.bounds()` plus PNG trim scale. That let rendered metadata and pixel crop repair inline geometry after Stage 1. | Inline badge material now requires an executable `INLINE` / `STORY_FLOW` ObjectPlan with visible visual, `ObjectPlan.file`, and positive `ObjectPlan.bounds`. The executor reads the planned file and sizes the inline object from planned bounds only; trimming changes the image payload, not HWPX placement size. |
| `converter/.../ResolvedBuildContext#isCompleteInlinePngByOwnershipPlan` / `InlineFrameHandler#findCompleteSimpleButtonLabelRender` | fixed / monitor | Complete-inline PNG helpers treated `OWNED_BY_PNG + PLACE_INLINE_PNG` as sufficient and could associate a complete render with an anchor through rendered `editableTextFrameIds`. That let rendered metadata stand in for the Stage 1 complete-PNG contract. | Complete inline PNG now requires `placement=INLINE`, `coordinateSpace=STORY_FLOW`, and `materialization=COMPLETE_PNG`. Simple-button complete render lookup keeps direct render-id matches, but child/anchor association uses only `ObjectPlan.sourceObjectIds`, `visualSourceObjectIds`, and `ownedTextFrameIds`, not rendered editable ids. |
| `converter/.../phase3/InlineFrameHandler` text-shell child TextFrame selection | fixed / monitor | Inline/floating text-shell execution selected child TextFrames from `RenderedGroup.editableTextFrameIds()` / `atomicOwnedTextFrameIds()` even when an ObjectPlan existed. That let rendered extraction metadata override or supplement Stage 1's text-slot ownership. | Text-shell execution now resolves child TextFrames from `ObjectPlan.ownedTextFrameIds` whenever a plan exists. Rendered editable/atomic ids remain only as a legacy fallback for unplanned conversion surfaces. |
| `converter/.../phase3/InlineFrameHandler#editableTextFrameIds` / `#isCoveredByInlineTextShellSourceBundle` | fixed / monitor | Shared inline-shell helpers still returned rendered editable ids when no matching plan was found, and source-bundle coverage used rendered `textOwner=hwpx_tf` / `visualOwner=indesign_png` plus source arrays to suppress later inline handling. This could hide missing inline text-shell ObjectPlans. | If Stage 1 ObjectPlans exist, editable ids and source-bundle coverage now come only from inline text-shell `ObjectPlan` fields (`textAction`, `placement`, `ownedTextFrameIds`, `sourceObjectIds`, `visualSourceObjectIds`). Rendered editable ids and rendered owner fields remain legacy-only when no ObjectPlans exist. |
| `converter/.../phase2/FramePlacer#editableInlineTextLengthForStory` | fixed / monitor | Overflow text-length estimation counted inline editable child text from `RenderedGroup.editableTextFrameIds()` / `atomicOwnedTextFrameIds()` when sizing HWPX text frames. That allowed rendered metadata to influence text-frame behavior after Stage 1 ownership was declared. | Inline child text length now comes from `ObjectPlan.ownedTextFrameIds` whenever a rendered group has a plan. Rendered editable/atomic ids are used only for legacy unplanned conversion surfaces. |
| `converter/.../phase2/FramePlacer#plannedOrLegacyEditableTextFrameIds` | fixed / monitor | The helper was plan-preferred, but still required rendered `hasEditableTextHiddenFromPng()` before reaching the plan branch, and fell back to rendered editable ids even when Stage 1 ObjectPlans existed elsewhere. That let rendered metadata affect text-length estimation after planning. | The loop now asks the helper first. If a plan exists, only `OWNED_BY_HWPX_TEXT` + `ObjectPlan.ownedTextFrameIds` contribute. If Stage 1 plans exist and no matching plan is found, rendered editable/atomic ids are ignored; they remain legacy-only when no ObjectPlans exist. |
| `converter/.../ASTRunConverter#shouldSkipInlineFrameByPlan` | fixed / monitor | Inline TextFrame suppression treated rendered textless-shell metadata as ownership evidence even when Stage 1 ObjectPlans were present. That could skip an inline frame based on extractor-owned child ids rather than the planned text owner. | When Stage 1 plans exist, inline frame suppression now checks planned text-shell `ObjectPlan.ownedTextFrameIds` only. Rendered editable/atomic ids remain limited to legacy conversion without ObjectPlans. |
| `converter/.../ASTRunConverter#isTextFrameOwnedByRenderedTextlessShell` | fixed / monitor | The inline TextFrame skip helper had a Stage 1 branch, but still iterated rendered groups first and found a plan through `ctx.findOwnershipPlanForRendered(rg)`. That made rendered shell rows a prerequisite for proving planned text-shell ownership. | When Stage 1 ObjectPlans exist, the helper now scans `ctx.ownershipPlans` directly for `OWNED_BY_HWPX_TEXT` text-shell plans owning the TextFrame. Rendered group metadata is not consulted until the legacy no-plan fallback. |
| `converter/.../resolved/ResolvedData#isHwpxOwnedTextFrame` / `#isTextOwnedByIndesignPng` | fixed / monitor | Shared text-owner helpers returned rendered `textOwner` / `editableTextFrameIds` facts even after Stage 1 ObjectPlans were present. Because these helpers feed AST inline child collection, FramePlacer, and inline-shell gates, rendered metadata could still override or supplement planned text-slot ownership. | When Stage 1 ObjectPlans exist, these helpers now answer only from `ObjectPlan.textAction`, `ObjectPlan.domId`, and `ObjectPlan.ownedTextFrameIds`. Rendered text-owner metadata is legacy-only for unplanned resolved inputs. |
| `converter/.../ASTInlineObjectBuilder#isHwpxOwnedChildTextFrame` | fixed / monitor | Inline child TextFrame collection first called the plan-first `ResolvedData#isHwpxOwnedTextFrame`, but when it returned false it still scanned `renderedFloatingItems[].editableTextFrameIds()` and treated a rendered extraction relation as HWPX text ownership. That could re-add child TFs after Stage 1 declined ownership. | If Stage 1 ObjectPlans exist, the helper now stops after the plan-first `ResolvedData` check. Rendered editable ids remain a legacy-only fallback for unplanned resolved inputs. |
| `converter/.../ASTOverlayBuilder#isOwnedByTextlessShell` | fixed / monitor | Background-group overlay wrapping skipped child TextFrames by scanning rendered shell metadata (`textOwner=hwpx_tf`, `visualOwner=indesign_png`, `editableTextFrameIds`) even when Stage 1 ObjectPlans were present. That let overlay text inclusion/exclusion be decided from extraction render metadata after planning. | When Stage 1 ObjectPlans exist, overlay child TextFrame suppression uses only the plan-first `ResolvedData#isHwpxOwnedTextFrame` result. Rendered shell metadata remains a legacy-only fallback for unplanned resolved inputs. |
| `converter/.../resolved/ResolvedData#shouldKeepVisualLabelTextEditable` / `#unregisterEditableLabelTextOwnersCoveredByShell` | fixed / monitor | Legacy visual-label repair used rendered `reason`, `textOwner`, child ids, style/text heuristics, and `VisualLayeringRules.isStructuredTextShellCandidate` to remove PNG text ownership. If reused after Stage 1, this could mutate text-slot ownership outside ObjectPlan. | The repair is now disabled whenever Stage 1 ObjectPlans exist. Planned conversions use ObjectPlan text ownership; rendered visual-label repair remains only for legacy resolved inputs before plan import. |
| `converter/.../ResolvedDataReader` / `ResolvedData#addRenderedFloatingItem` object-plan material preservation | fixed / monitor | Rendered material registration could drop malformed-looking label backdrops, live title decoration duplicates, or duplicate visual page objects using rendered `reason`, text-owner fields, and bounds before Stage 1 ObjectPlans were imported. That could make a valid ObjectPlan lose its executable PNG material before planning even began. | When `resolved.json` has a sibling `object-plans.json`, the reader marks the data as ObjectPlan-backed and `addRenderedFloatingItem` preserves all rendered floating material instead of running legacy reason/geometry cleanup. Legacy cleanup remains available only for old resolved inputs without ObjectPlans. The policy checker fails if this preservation gate is removed. |
| `converter/.../ASTStoryConverter#isOwnedByRenderedTextlessShell` | fixed / monitor | Story conversion skipped inline TextFrames by reading rendered textless-shell editable ids after Stage 1 could already have declared the text owner. | When Stage 1 plans exist, story conversion now treats an inline TextFrame as shell-owned only when a planned text-shell ObjectPlan owns that TextFrame. Rendered editable/atomic ids remain legacy-only. |
| `converter/.../ASTStoryConverter#isOwnedByRenderedTextlessShell` direct plan scan | fixed / monitor | The story skip helper had a Stage 1 branch, but still iterated rendered groups and looked up plans through `ctx.findOwnershipPlanForRendered(rg)`. That made rendered shell rows part of the proof path for planned text ownership. | When Stage 1 ObjectPlans exist, the helper now scans `ctx.ownershipPlans` directly for `OWNED_BY_HWPX_TEXT` text-shell plans owning the TextFrame. Rendered textless-shell metadata is consulted only in the no-plan legacy fallback. |
| `converter/.../ASTTableConverter#findTextHiddenShellForEditableTextFrame` | fixed / monitor | Table-cell inline shell normalization found shell backdrops from rendered editable ids and then applied rendered PNG fill to an inline text frame. The first fix still proved Stage 1 ownership by iterating rendered groups and looking up the matching plan through each rendered row. | When Stage 1 plans exist, table-cell shell fill first scans ObjectPlans directly for a text-shell plan that owns the TextFrame. Rendered groups are then used only as material lookup by `ObjectPlan.file`; rendered editable ids remain legacy-only. |
| `converter/.../stage3/VisualPlacementExecutor` extraction metadata fallback | fixed / monitor | ASTFigure extraction trace fields fell back from missing ObjectPlan fields to `RenderedGroup` candidate/pass/slot metadata. That could hide incomplete planned trace/source-slot identity in Stage 3 output. | Visual placement now writes extraction candidate/pass/slot only from the ObjectPlan. Missing planned trace fields remain visible instead of being repaired from rendered metadata. |
| `converter/.../ResolvedBuildContext#findOwnershipPlanForRendered` / `VisualPlacementExecutor` | confirmed violation / fixed | Rendered lookup could return a visible ObjectPlan with missing materialization/action/layer/placement/coordinateSpace, and Stage 3 tracing then crashed or could attempt execution from an incomplete ownership contract. | Rendered visual lookup now ignores visible plans with incomplete execution fields, and `VisualPlacementExecutor` refuses to place any visual without a complete visible ObjectPlan. Trace TSV enum fields are null-safe so the defect remains diagnostic instead of becoming a crash. |
| `converter/.../ResolvedBuildContext#shouldDropVisualByOwnershipPlan` | fixed / monitor | When no matching ObjectPlan was found, the shared Stage 3 visual drop helper still used rendered `textOwner=hwpx_tf`, `hasEditableTextHiddenFromPng`, and rendered editable ids to suppress a visual. That could hide a missing ObjectPlan by treating rendered extraction metadata as a post-plan ownership decision. | If Stage 1 ObjectPlans exist, the helper now answers only from the matching ObjectPlan. The rendered text-owner drop fallback is legacy-only for conversion inputs with no ObjectPlans. |
| `converter/.../phase4/TableBuilder#markAllDescendantsVisualHandled` shared-file suppression | fixed / monitor | Table/page-object handling suppressed every rendered item that shared the same PNG file as a handled group. File identity is a material artifact, not a source-bundle ownership slot, and could hide unrelated visible owners. | The shared-file suppression step was removed. The method now suppresses only the handled source group id and its source-tree descendants. |
| `converter/.../phase3/StoryLoader#applyResolvedCellInlineGraphicRescueAnchors` paragraph retargeting | fixed / monitor | Resolved table-cell inline graphic rescue moved prefix-marker anchors to the paragraph whose other inline visual bounds had the nearest Y coordinate. That reopened story-flow placement after Stage 1 using geometry proximity. | Rescue insertion now uses the resolved run's own paragraph index. The Y-nearest paragraph retargeting helpers were removed; paragraph placement must come from source/resolved story flow, not coordinate proximity. |
| `converter/.../phase3/StoryLoader` inline graphic rescue reason strings | fixed / monitor | Table-cell inline rescue allowed special prefix-marker handling based on `ObjectPlan.reason` values such as `inline_graphic_only` and `paper_inline_anchor_uses_page_material_slot`. `reason` is diagnostic text, not an executable ownership field. | Rescue eligibility now uses the inline ObjectPlan contract and requires non-empty `sourceObjectIds`. Source-less inline rescue is left visible as a Stage 1 planning defect instead of being admitted by reason strings. |
| `converter/.../phase3/InlineFrameHandler` inline text-shell reason strings | fixed / monitor | Inline text-shell execution used `ObjectPlan.reason` and PNG filename prefixes to decide simple-label vertical alignment, nested composite parent handling, and duplicate plan priority. That made diagnostic strings/material filenames executable ownership signals. | Simple-label alignment now relies on the planned inline text-shell contract and label-layer/text-bounds relation. Nested composite parent handling uses structured ObjectPlan fields (`placement`, `visualAction`, `textAction`, visible visual, source ids, owned TextFrame ids), and plan priority no longer reads `reason`. |
| `converter/.../stage3/VisualLayeringRules` / `resolved/ResolvedData` shell candidate reason strings | fixed / monitor | Shared label/text-shell candidate classification read rendered `reason` fragments such as `text_hidden`, `visual_shell`, `decoration`, and `complex_graphic`. That made a diagnostic explanation string participate in shell ownership classification. | Shell candidate classification now uses structured extractor/source metadata: `slotRole`, `compositeRole`, `planPassId`, `atomicObjectKind`, and `textHiddenBeforeExport`/editable TextFrame ids. An unused `FramePlacer` reason helper was removed. Remaining `ResolvedData` legacy reason paths are still audit targets. |
| `converter/.../resolved/ResolvedData` atomic marker fallback | fixed / monitor | Atomic marker/textless-shell handling accepted old `reason` values such as `visual_label_text_hidden_shell`, `leaf_group_text_hidden_shell`, `editable_composite_text_hidden_shell`, and `inline_graphic_only`, then rebuilt atomic source membership from resolved source-tree geometry. That allowed Java to recreate an atomic ownership bundle after extraction planning. | Atomic marker handling now requires structured `atomicObjectKind` plus `atomicSourceObjectIds` / `atomicOwnedTextFrameIds`. The source-tree bundle reconstruction cache and reason-based inline-complete marker fallback were removed; missing atomic metadata remains a Stage 1/extraction metadata defect. |
| `converter/.../resolved/ResolvedData#registerIndesignPngTextOwner` visual-label text ownership | fixed / monitor | `indesignPngTextOwnerFrameIds` could be populated from rendered `textOwner=indesign_png` plus simple marker text heuristics, while skipping/repairing visual-label cases by `reason`. That allowed text ownership to be inferred after extraction from text shape rather than an explicit source contract. | PNG-owned text registration now requires structured `atomicObjectKind=COMPLETE_PNG` and uses `editableTextFrameIds` / `atomicOwnedTextFrameIds` only. The simple-marker text heuristic no longer grants PNG text ownership. |
| `converter/.../phase2/FramePlacer` floating HWPX text bounds bridge | fixed / monitor | Floating HWPX text plans without executable PAGE bounds emitted a warning but still fell back to TextFrame `geometricBounds()` / `pageRelativeBounds()` for placement. That let Stage 2 repair missing text geometry after planning. | Floating HWPX text placement now requires a `HWPX_TEXT` ObjectPlan with executable PAGE bounds. Missing or incomplete text plans emit `STAGE2_TEXT_PLAN_MISSING_EXECUTABLE_BOUNDS` and are skipped instead of using legacy TextFrame geometry. |
| `converter/.../phase2/FramePlacer#applyGroupBackgroundShapeStyle` | fixed / monitor | Text-frame block style absorption accepted ObjectPlan style-source ids, but then used TextFrame/source overlap scoring to choose the actual shell/style source during Stage 2. That reintroduced geometry-based ownership selection in an executor. | The executor now applies only the `styleSourceObjectIds` declared by Stage 1, in declared order, and no longer uses overlap or bounds to pick a style owner. Source geometry is not used to repair or infer text-frame style ownership. |
| `converter/.../phase3/InlineFrameHandler#loadCompleteSimpleButtonLabelInlineObject` | fixed / monitor | Complete simple-button labels could still be placed without an ObjectPlan by checking nearby text geometry/story context, and planned labels sized themselves from rendered bounds, PNG trim scale, or PNG DPI fallback. | Complete simple-button PNG execution now requires an inline `COMPLETE_PNG` ObjectPlan with `STORY_FLOW` bounds and file. The executor uses only `ObjectPlan.file` and `ObjectPlan.bounds`; PNG trim changes only the image payload, not the HWPX inline size. Rendered-bounds placement and geometry/story-context fallback helpers were removed. |
| `converter/.../phase3/InlineFrameHandler` native inline text-shell fallback | fixed / monitor | Native inline text-shell execution could recover missing shell bounds from source `geometricBounds()` and could synthesize a shell from an inline TextFrame's own fill/stroke when no explicit shell ObjectPlan existed. Planned inline shell image material also had a fallback from missing `ObjectPlan.file` to the rendered group's file. | Native inline text-shell execution now requires an executable `INLINE` / `STORY_FLOW` / `NATIVE_SOURCE_SHAPE` text-shell ObjectPlan with positive bounds. The TextFrame-fill/stroke synthetic shell fallback was removed, and inline shell material loading now uses only `ObjectPlan.file`; visible shell material must be planned in Stage 1. |
| `converter/.../phase3/InlineFrameHandler` inline text-shell fragment split | fixed / monitor | A single planned inline text-shell could be materialized as multiple inline image/text fragments by scanning `visualSourceObjectIds`, recomputing child bounds, alpha-knocking the shell PNG, and cropping sub-images in Stage 3. That created fragment placement/crop geometry outside ObjectPlan. | The executor-local fragment split path was removed from inline anchor and table-cell flows. Until Stage 1 emits explicit fragment ObjectPlans/files/bounds, a planned text shell is executed as the single planned shell carrier rather than being split by Stage 3 source geometry. |
| `converter/.../phase3/InlineFrameHandler` closed inline carrier flow | fixed / monitor | Closed inline carrier handling filled missing `hiddenVisualSourceObjectIds` from `RenderedGroup.hiddenVisualSourceObjectIds()` and computed visual/text ordering by scanning rendered/source child geometry. That let Stage 3 recover omitted-child and flow-order facts outside ObjectPlan. | Closed carrier expansion now consumes only `ObjectPlan.hiddenVisualSourceObjectIds` plus `inlineFlowSourceObjectIds`. Missing hidden ids, flow order, or planned bounds leave the carrier unexpanded and diagnostic rather than falling back to rendered metadata or geometry sorting. |
| `converter/.../OwnershipPlanner#normalizeVisualOnlyTextShellsDoNotOwnTextSlots` / `OwnershipPlanValidator` visual-only shell checks | fixed / monitor | Text-shell cleanup and validation trusted diagnostic `reason` strings such as `visual_only`, `story_flow_inline_shell_visual_only`, `composite_carrier_visual_only`, and `cross_page_text_shell_visual_fragment` to clear or accept text-slot ownership. That let reason text decide whether a shell could retain or drop `ownedTextFrameIds`. | Text-slot cleanup now follows structured ObjectPlan fields: `DROP_TEXT` + `PLACE_TEXT_SHELL` plans without visible editable text sources cannot retain text ownership ids, regardless of reason text. Validation no longer accepts visual-only reason strings as a shortcut; any text-frame sources on a `DROP_TEXT` shell must be accounted for by real HWPX text-owner plans. The policy checker fails if the visual-only reason helper or shortcut returns. |
| `converter/.../phase4/TableBuilder#renderTableAsImage` | fixed / monitor | Legacy whole-table PNG execution validated that a matching ObjectPlan existed, but still used `RenderedGroup.file()` and table-derived dimensions when plan fields were incomplete. That could materialize a table PNG with rendered metadata rather than the executable ObjectPlan. | Whole-table PNG execution now requires a `FLOATING` / `PAGE` `PLACE_FLOATING_PNG` plan with PNG/vector materialization, `ObjectPlan.file`, and positive `ObjectPlan.bounds`. The executor reads the file and dimensions from the plan only; missing planned material leaves the table on the ASTTable path. |
| `scripts/indd/planner_bundles.jsx` / `object_plans.jsx` complex textless background shell fragments | fixed / monitor | Full-range extraction could classify a textless complex graphic frame as `BUNDLE_NARROWER_THAN_CLUSTER` because hidden sibling/group descendants existed outside the visible shell slot. The visible source ids were explicit, but `exportSourceObjectIds` named only the root export target, so the ObjectPlan stayed `NEEDS_VISIBLE_SLOT_EXPLICITNESS` and source coverage failed before HWPX generation. | Stage 1 now treats `pass.complex_graphic_frames` textless `SHELL_SLOT` background fragments with an explicit `exportTargetObjectId` as a closed root-export visible fragment. `hiddenVisualSourceObjectIds` records the omitted cluster descendants, while `visualSourceObjectIds` records the owned visible material. The import-ready contract accepts root export only for this structured textless background shell case, not for arbitrary partial bundles. |
| `scripts/indd/planner_bundles.jsx` page-local visual crop contract | fixed / monitor | Page-local visual fragments could carry a broader source extent only as diagnostic `renderSourceBounds`, while `cropSourceBounds` stayed empty. Stage 3 correctly refuses to crop from `renderSourceBounds`, so spread-cross/page-clipped PNGs could be placed untrimmed or with mismatched intrinsic pixels. | Planner bundles now compute a Stage 1 source extent once and write it separately to `renderSourceBounds` for provenance and to `cropSourceBounds` as the executable crop contract when it contains and is materially larger than planned `bounds`. Java Stage 3 still executes only explicit `ObjectPlan.cropSourceBounds` or legacy explicit `RenderedGroup.cropSourceBounds`; it does not derive crop from render provenance. |
| `scripts/indd/execution_candidates.jsx` ObjectPlan bridge rebuild fields | fixed / monitor | After ObjectPlan import, source-slot canonicalization can rebuild planner bundles from execution candidates. The bridge copied executable visual fields but left Stage 1 contract fields such as `mode`, `clusterRelation`, and omitted cluster ids behind, so a previously import-ready slot-only root-export shell could degrade back into `NEEDS_MIGRATION_POLICY` during the rebuild. | The bridge now carries ObjectPlan `mode`, role fields, cluster relation, cluster source ids, omitted cluster ids, and cluster profile flags into execution candidates. While the legacy execution-candidate bridge remains, rebuilds preserve the Stage 1 slot-only ownership contract instead of recomputing it from a thinner candidate row. |
